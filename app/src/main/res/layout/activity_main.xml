<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <android.opengl.GLSurfaceView
        android:id="@+id/surfaceView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:id="@+id/statusBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:background="#99000000"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="12dp">

        <TextView
            android:id="@+id/statusText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            tools:text="0 枚（TRACKING 0）" />

        <!-- カバレッジ表示の入切。用途が違う人には無意味なので消せる。 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/guideButton"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="0dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:text="@string/coverage_toggle" />

        <!-- 撮影中にも手順を読み返せるようにする。 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/helpButton"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:minWidth="0dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:text="@string/help" />
    </LinearLayout>

    <!--
        撮った視点方向のリング。カメラ映像の上に重ねるだけで、撮影にも出力にも
        影響しない。タップすると被写体の中心を取り直す。
    -->
    <com.example.arcorefetcher.coverage.CoverageView
        android:id="@+id/coverageView"
        android:layout_width="132dp"
        android:layout_height="132dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="72dp"
        android:layout_marginEnd="12dp"
        android:clickable="true"
        android:contentDescription="@string/coverage_description"
        android:focusable="true" />

    <!--
        シャッターは画面の中央下。カメラアプリと同じ位置に置かないと、
        構えたまま親指で押せない。書き出しは端へ逃がして誤爆を避ける。
    -->
    <FrameLayout
        android:id="@+id/controlBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="#99000000"
        android:paddingTop="14dp"
        android:paddingBottom="14dp">

        <com.example.arcorefetcher.ShutterButton
            android:id="@+id/shutterButton"
            android:layout_width="@dimen/shutter_size"
            android:layout_height="@dimen/shutter_size"
            android:layout_gravity="center"
            android:contentDescription="@string/shutter" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/finishButton"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|end"
            android:layout_marginEnd="16dp"
            android:text="@string/finish" />
    </FrameLayout>

    <!--
        撮れた瞬間に画面全体を一瞬光らせる面。**最前面に置く**（撮影ボタンごと光る）。
        クリックを取らない素の View なので、光っている間もタッチは下へ抜ける。
    -->
    <View
        android:id="@+id/flashView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0"
        android:background="@color/capture_flash"
        android:visibility="gone" />
</FrameLayout>
